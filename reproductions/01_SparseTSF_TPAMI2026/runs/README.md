# 云端实验记录目录

当前没有本项目训练结果。每次实验单独建立 `runs/<run_id>/`，建议包含：

- `run.json`：论文任务、数据来源及哈希、代码提交、输入输出长度、随机种子、环境版本和运行状态。
- `console.log`：完整标准输出和错误。
- `metrics.json`：本次实测指标，以及对应论文表格的位置与评价口径。
- `checkpoints/`：本次训练生成的模型（如需回传）。

原论文复现和 ERCO 统一基线分开记录。`completed` 只代表程序完成，不自动意味着达到论文效果。不要保存账户令牌、密码或完整环境变量。

